RenderPasses contains the original way I implemented custom shaders
RenderPasses_v2 (will add soon) shows a newer tidier way of implementing custom shaders

You don't have to do things the way I have, these are just ways I've found to make it easier for myself 
in terms of development and maintenance 
